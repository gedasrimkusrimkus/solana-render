services:
  - type: web
    name: wallet-tracker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python simple_tracker.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0